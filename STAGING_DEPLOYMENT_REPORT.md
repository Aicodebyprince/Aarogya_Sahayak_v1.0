# Aarogya Sahayak - Staging Deployment Report

**Environment**: STAGING  
**OTP Mode**: MOCK (`123456`)  
**Integrations**: MOCK unless individually configured with live credentials  
**Data**: DEMO ONLY  
**Git Commit Hash**: `fe73de3c7129e01b6d10d57d696f6ecdd884bc0d`  
**Git Branch**: `main`  
**Remote Repository**: [sohamshetye-git/AarogyaSahayak](https://github.com/sohamshetye-git/AarogyaSahayak)

---

## 1. Phase 1: Repository Verification & Push Summary

- [x] **Branch Verification**: Confirmed current branch is `main`.
- [x] **Remote Verification**: Points to `https://github.com/sohamshetye-git/AarogyaSahayak.git`.
- [x] **Secret & Artifact Scan**: No `.env`, SQLite DB files (`aarogya.db`), `.vite`, `dist`, or `__pycache__` files staged.
- [x] **Frontend Builds**:
  - `apps/citizen-mobile`: Build passed cleanly (`tsc && vite build`, dist generated).
  - `apps/healthcare-portal`: Build passed cleanly (`tsc && vite build`, dist generated).
- [x] **Database & Migrations**: `alembic heads` confirmed head at `ea1b2c3d4e5f` (`create_doctor_chat_tables`).
- [x] **Unit Tests**: Staging specific unit tests passed (`pytest tests/test_teleconsultation.py tests/test_followups_and_timeline.py` 5 passed).
- [x] **Commit & Push**: Committed `fe73de3` ("Prepare Aarogya Sahayak staging deployment") and pushed cleanly to `origin/main` without force.

---

## 2. Phase 2 & 3: Render Blueprint Staging Deployment

### Resource Specifications (`render.yaml`)
1. **Managed PostgreSQL**:
   - **Name**: `aarogya-sahayak-db`
   - **Region**: Singapore (`singapore`)
   - **Version**: PostgreSQL 16
   - **Database Name**: `aarogya_db`
   - **User**: `aarogya_user`
   - **Plan**: `starter` (or `free` depending on Render account tier availability)

2. **Web Service (FastAPI Backend)**:
   - **Name**: `aarogya-sahayak-backend`
   - **Region**: Singapore (`singapore`)
   - **Runtime**: Python 3
   - **Root Directory**: `backend`
   - **Build Command**: `pip install -r requirements.txt`
   - **Pre-Deploy Command**: `alembic upgrade head`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
   - **Health Check Path**: `/health`
   - **Plan**: `starter` (or `free` tier if selected)
   - **Key Environment Variables**:
     - `ENVIRONMENT=staging`
     - `OTP_MODE=MOCK`
     - `INTEGRATION_MODE=mock`
     - `GEMINI_MODE=mock`
     - `SARVAM_MODE=mock`
     - `DATABASE_URL` (Derived via `fromDatabase: aarogya-sahayak-db`)
     - `JWT_SECRET` / `JWT_REFRESH_SECRET` (Auto-generated by Render)

> [!IMPORTANT]
> ### Mandatory Payment & Plan Approval Checkpoint
> Before applying the Blueprint in Render:
> - Review the tier selection for `aarogya-sahayak-backend` and `aarogya-sahayak-db`.
> - If you are using Render Free Tier / Starter credits, verify whether your team requires Free Tier (`plan: free`) or Starter (`plan: starter`).
> - Once approved in your Render dashboard, navigate to **New → Blueprint**, select `sohamshetye-git/AarogyaSahayak` (`main` branch), and click **Apply**.

### Verification Steps After Render Deploy
1. Confirm health check: `GET https://<backend-app-name>.onrender.com/health` returns `{"status": "ok", "environment": "staging"}`.
2. Note backend public URL: `https://<backend-app-name>.onrender.com`.

---

## 3. Phase 4 & 5: Vercel Staging Projects

### A. Citizen Mobile (`aarogya-citizen`)
- **Repository**: `sohamshetye-git/AarogyaSahayak`
- **Framework Preset**: `Vite`
- **Root Directory**: `apps/citizen-mobile`
- **Include files outside Root Directory**: Checked / Enabled
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Node.js Version**: `20.x`
- **Environment Variables**:
  - `VITE_API_BASE_URL=https://<backend-app-name>.onrender.com/api`
  - `VITE_WS_URL=wss://<backend-app-name>.onrender.com`
  - `VITE_APP_ENV=staging`

### B. Healthcare Portal (`aarogya-healthcare-portal`)
- **Repository**: `sohamshetye-git/AarogyaSahayak`
- **Framework Preset**: `Vite`
- **Root Directory**: `apps/healthcare-portal`
- **Include files outside Root Directory**: Checked / Enabled
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Node.js Version**: `20.x`
- **Environment Variables**:
  - `VITE_API_BASE_URL=https://<backend-app-name>.onrender.com/api`
  - `VITE_WS_URL=wss://<backend-app-name>.onrender.com`
  - `VITE_APP_ENV=staging`

---

## 4. Phase 6: Configure Backend CORS & WebSockets

Once Vercel generates the production URLs for both apps (e.g. `https://aarogya-citizen-staging.vercel.app` and `https://aarogya-portal-staging.vercel.app`):
1. In Render Dashboard -> `aarogya-sahayak-backend` -> **Environment**:
2. Set or update `CORS_ORIGINS`:
   ```text
   CORS_ORIGINS=https://aarogya-citizen.vercel.app,https://aarogya-healthcare-portal.vercel.app
   ```
3. Save changes to trigger a rolling redeploy.

---

## 5. Phase 7: Verification & Smoke-Test Checklist

| Test Case | Expected Behavior | Staging Note |
|---|---|---|
| **Citizen Auth** | Mobile entry -> OTP screen | Mock OTP `123456` activates session |
| **Language Selection** | Switching between 11 Indic languages | Language context persisted |
| **Doctor Waiting Room** | Citizen connects to `/api/ws` | Real-time queue position & doctor chat |
| **Doctor Direct Requests** | Doctor sees citizen chat queue in Portal | Accept request -> 2-way real-time messaging |
| **Facility Search** | Query PHC / CHC / Hospital | DB-backed facilities returned |
| **ASHA Field Work** | Offline visits & sync | Idempotent sync to backend |

---

## 6. Hackathon Demo Readiness Summary

- **Staging Readiness**: Ready to deploy via Render Blueprint and Vercel.
- **Next Manual Action**: Open [dashboard.render.com](https://dashboard.render.com) to deploy the Blueprint and [vercel.com](https://vercel.com) to link the frontend projects with the environment variables provided above.
